# salescrmdatagenerator
Need CRM-like sales data but don't want to violate PII or NDAs? Use the Sales CRM Dummy Data Generator!

This simple R script creates sales data. Requires the randomNames library.
